# 사용자 패턴 분석

> 자동 생성됨 (2026-04-28). pattern-log.jsonl 기반.

## 요청 빈도

- "bug-fix" 요청이 전체의 56% (5/9)
- "feature" 요청이 전체의 33% (3/9)

## 자주 반복되는 명령 순서

- "ticket → ticket" 순서가 27회 반복
- "ticket → run" 순서가 6회 반복
- "run → ticket" 순서가 5회 반복
- "status → ticket" 순서가 4회 반복
- "feature → feature" 순서가 4회 반복
- "ticket → status" 순서가 3회 반복
- "ticket → feature" 순서가 3회 반복
- "run → run" 순서가 3회 반복
- "doctor → ticket" 순서가 2회 반복
- "feature → ticket" 순서가 2회 반복

## 실패 패턴

- 실패 "Failed at phase: plan": 3회
